SADATrack: A Framework for Building Tennis Analytics
-
The project is split between three branches:
- `swing-classifier`:
- `ball-tracking-system`:
- `stable-version`: Contains the code and notebook that put everything together. Takes rally videos as input and returns the swing predictions.

